import 'package:flutter/material.dart';

class RecargaCuentaPage extends StatelessWidget {
  const RecargaCuentaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
